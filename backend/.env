# Database Configuration
DATABASE_URL=postgresql://scanner_user:Yitayh2334@localhost/ai_scanner

# Security - CHANGE THESE IN PRODUCTION!
# Generate strong keys with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_ME_IN_PRODUCTION_32_BYTE_SECRET_KEY
JWT_SECRET_KEY=CHANGE_ME_IN_PRODUCTION_32_BYTE_JWT_SECRET_KEY

# CORS - Frontend origin (change for production)
CORS_ORIGIN=http://localhost:5173

# Debug mode - Set to False in production
DEBUG=True

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO